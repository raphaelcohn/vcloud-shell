#!/usr/bin/env vsh
api xml extension

#defaults_format_headings=with-heading format id-tsv extension "${downloads[0]}"
#format id-tsv extension "${downloads[0]}"
#format id-bash extension "${downloads[0]}"

#format links admins "${downloads[0]}"
